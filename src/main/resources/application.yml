spring:

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://developeconnect.cy9wxcxnfmem.us-east-2.rds.amazonaws.com/develope_connect?serverTimezone=Asia/Seoul
    username: root
    password: qweasd12


  jpa:
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create  #create-drop : SessionFactory가 시작될 때 Drop, Create, Alter 종료될때 Drop => 스프링부트 서버가 시작될 때마다 테이블들을 새로 생성
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  sql:
    init:
      mode: always   #data.sql이 실행되면서 초기값 설정해주는 설정

logging:
  level:
    ms.silvernine: DEBUG

#JWT 관련 설정
jwt:
  header: Authorization
  #HS512 알고리즘을 사용할 것이기 때문에 512bit, 즉 64byte 이상의 secret key를 사용해야 한다.
  #echo 'silvernine-tech-spring-boot-jwt-tutorial-secret-silvernine-tech-spring-boot-jwt-tutorial-secret'|base64
  secret: c2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQtc2lsdmVybmluZS10ZWNoLXNwcmluZy1ib290LWp3dC10dXRvcmlhbC1zZWNyZXQK
  token-validity-in-seconds: 86400